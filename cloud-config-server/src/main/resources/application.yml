eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
server:
  port: 8888
spring:
  config:
    name: config-server
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/karasuno07/alpaca-config-server.git
#          username:
#          password: